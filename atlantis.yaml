version: 3
projects:
  - name: default
    dir: .
    workspace: default
    autoplan:
      when_modified: ["*.tf", "*.tfvars"]
      enabled: true

  - name: atlantis-test
    dir: .
    workspace: atlantis-test
    autoplan:
      when_modified: ["*.tf", "*.tfvars"]
      enabled: true
